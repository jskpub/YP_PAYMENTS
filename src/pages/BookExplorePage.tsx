import React from 'react';
import { BizMallGridPage } from './BizMallGridPage';

/** B2B 프로토타입 전용 - Image 1 (BizMallGridPage) 레이아웃으로 대체 */
export const BookExplorePage: React.FC = () => {
  return <BizMallGridPage menu='ALL' />;
};
